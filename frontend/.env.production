# Entorno de Producción del Frontend (FALLBACK)
# 
# Este archivo se usa como FALLBACK al compilar localmente sin argumentos de build.
# 
# Para deploy en Coolify/Docker:
#   -> Establecer VITE_API_URL como Build Variable en Coolify (recomendado)
#   -> Este valor será ignorado si se proporciona un argumento de build
#
# Para desarrollo local:
#   -> Crear .env.local con: VITE_API_URL=http://localhost:3000
#
# Fallback por defecto (cambiar para builds de producción locales):
VITE_API_URL=http://localhost:3000
